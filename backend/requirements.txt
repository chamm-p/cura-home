# ─── Web Framework ───
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# ─── Database ───
sqlalchemy[asyncio]==2.0.40
asyncpg==0.30.0
alembic==1.15.2
greenlet==3.1.1

# ─── Auth ───
python-jose[cryptography]==3.4.0
# Generic OIDC via Authlib — Discovery + JWKS-Verifikation, anbindbar an Keycloak.
Authlib==1.6.5

# ─── Encryption ───
cryptography==44.0.2

# ─── Validation / Config ───
pydantic==2.11.7
pydantic-settings==2.10.1
email-validator==2.2.0
python-dotenv==1.1.1

# ─── HTTP Client (LLM-Provider + OIDC) ───
httpx==0.28.1

# ─── PDF (Inventarliste + QR-Etiketten) ───
weasyprint==63.1

# ─── QR-Codes (Etikettengenerierung) ───
qrcode[pil]==8.0

# ─── Bilder (Foto-Resize/Thumbnails) ───
pillow==11.1.0

# ─── JSON-Repair (robustes LLM-JSON) ───
json-repair==0.30.3
